{% extends 'base.html.twig' %}

{% block title %}Modifier un film{% endblock %}

{% block body %}
<div class="container py-5">
    <h1 class="h3 text-primary mb-4">✏️ Modifier un film</h1>

    {{ include('film/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="d-flex justify-content-between align-items-center mt-4">
        <a href="{{ path('app_film_index') }}" class="btn btn-outline-secondary">
            ← Retour à la liste
        </a>

        {{ include('film/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
